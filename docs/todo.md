# todo.md — mini-deer-flow 进度与待办

> **基线**：`cd backend && make test && make lint` → **1658 passed, 1 skipped, 0 lint**。
> **三交付** = 代码 + hermetic 测试（`test/test_<module>.py`）+ 学习文档（`docs/<module>.md`）。
> **设计规格**（文件清单 / 依赖 / 红线）查 [ALIGNMENT_OUTLINE.md](ALIGNMENT_OUTLINE.md) Part C；**全模块学习文档**见 [README.md](README.md)（#1–#28）。

## 图例

✅ 完成 · 🔶 部分 · 📋 规格 · ⬜ 未开始

---

## 一、已完成（主线对标全部完成）

**Phase 0–8 全部 ✅**，对齐 deer-flow v1.2「全面对标、不裁剪核心功能」。harness 层核心（模型/沙箱/子代理/记忆/技能/MCP/联网/工具/上传/中间件 23 步/agent 装配/运行管理/Store/集成）零差距，1477 测试 + 28 篇文档。

| Phase | 模块 | 文档 |
|-------|------|------|
| 0 | M-build 工程化 · M0 config · M1 utils · M2 reflection · M3 user_context | build / testing-setup / config / utils / user_context |
| 1 | M-models · M4 persistence · M5 checkpointer · M6 events/store · M7 journal · M8 stream_bridge · M9 serialization | models / persistence / checkpointer / run_event_store / run_journal / stream_bridge / serialization |
| 2 | M10 sandbox(7 工具) · M10b AIO 沙箱 · M11 subagents · M12 tracing · M22 agents_config | sandbox / aio_sandbox / subagents / tracing / agents_config |
| 3 | M13 memory | memory |
| 4 | M14 skills | skills |
| 5 | M20 mcp · M21 community(12 provider) · M15 tools(9 内置) | mcp / community / tools |
| 5.5 | M23 uploads | uploads |
| 6 | M16 middlewares（23 步生产链） | middlewares |
| 7 | M17 agent（SDK + config 双入口 + custom-agent 分支） | agents |
| 8 | M18 runs（RunManager + worker）· M19 store + 集成装配（lifespan / langgraph.json / config.example） | runs / runtime_store / architecture |

**质量加固**：Phase 1（M4–M9）+ Phase 7–8（M17/M18/M19/集成）已完成四维审查（设计 / bug / 适配 / 测试文档），结论均为「零严重 bug + 测试缺口 + 文档措辞，已补齐」。⚠️ **该结论基于旧上游**——2026-06-26 deer-flow 上游更新后需按 §三 全量重审，结论不再假设成立。

**补缺**：`models/vllm_provider.py`（§2.2 原 🔴高）已补——移植 `VllmChatModel`（继承 `ChatOpenAI`，保住 vLLM 非标准 `reasoning` 字段在非流式 / 流式 / 多轮请求三处）+ 21 项 hermetic 测试（`test/test_vllm_provider.py`）+ `docs/models.md` 补全（§4.6 为什么 / §5 逐文件 / §7.7 示例 / §9-Q7 排错）+ `config.example.yaml` 新增「路径 D」vLLM 推理模型示例。基线 1477 → **1498 passed**, 0 lint。

**M16 middlewares 六维重审（2026-06-27）**：补齐 3 个缺失中间件——`input_sanitization_middleware`(#1, #3630 提示词注入防御) / `system_message_coalescing_middleware`(#20, #3711 合并 SystemMessage) / `token_budget_middleware`(#23, 单 run token 预算) + 新 `config/token_budget_config.py` + AppConfig/`__init__` 接线 + `is_dynamic_context_reminder` 对齐上游（认 HumanMessage + SystemMessage）。链 23→26 步（对齐上游 AGENTS.md，mini 跳过 #8 Guardrail）。逐文件 diff 确认 **#3746（DanglingToolCall ID-swap）/ #3709（LoopDetection 位置回退）mini 此前已含**（差异仅文档措辞，无逻辑差）。+ 72 项 hermetic 测试（3 个新 test 文件 + `test_middlewares.py` 链序回归）+ `docs/middlewares.md` 全重写（26 步链 + 数据流架构图 + 25 文件逐个分析 + 排错 FAQ）+ `config.example.yaml` `token_budget` 段。基线 1498 → **1570 passed**, 0 lint。详见 §3.1/§3.2（M16 行已 ✅）。

**M11 subagents 六维重审（2026-06-27）**：9 文件逐个 diff 最新上游——公共同构（`__init__`/`config`/`registry`/`executor`/`status_contract`/`token_collector`/3 builtins），差异**几乎全是中英 docstring**。确认 mini **均已含**：#34 持久隔离事件循环（`_isolated_subagent_loop` daemon 线程 + `atexit` 优雅关闭）/ #35 单 scheduler pool（**非**双池——AGENTS.md 误记，实际 mini↔上游都是唯一 `_scheduler_pool(max_workers=3)`，`MAX_CONCURRENT_SUBAGENTS=3`）/ 5 状态契约（#3146 `status_contract` + 共享 fixture）/ `checkpointer=False`（#3559）/ 协作取消在 astream 边界 / 单 SystemMessage 合并（#2701 `_load_skill_messages`）/ 超时终态原子（#2583 `try_set_terminal` + `_state_lock`）/ #2414 / #3610（max_turns=150, timeout 30min）。补 **1 项性能对齐 #3687**：流式 AI 消息去重由每 chunk `any(...)` O(n) 扫描（整轮 O(n²)，max_turns=150 显著）改为 `seen_message_ids` 集合 O(1) 查 + append 后维护（语义不变）。**#3658（按模型归桶 token）归 M18**：mini journal 全缺 `_record_model_usage`，跨 token_collector+journal+worker+persistence 4 模块，域特性不零碎半挂。+ 2 项 hermetic 测试（`test_execute_seen_set_seeded_from_holder_messages` / `test_execute_dedups_many_distinct_ids_and_repeats`，锁住集合预建 + 维护）+ `docs/subagents.md` 全重写（新增内部数据流图 task工具→线程池→隔离循环→astream→状态机 + 9 文件逐个分析「做什么/为什么单独」）。**遗留 1 项已闭环**：middlewares Uploads/ThreadData 顺序（§3.1-D，2026-06-28 已对齐上游 Uploads→ThreadData→Sandbox，见 §四 已完成）。基线 1570 → **1572 passed**, 0 lint。详见 §3.1/§3.2（M11 行已 ✅）。

**M15 tools 六维重审（2026-06-27）**：15 文件逐个 diff 最新上游——确认 M15 五大关注点 **mini 均已含**：#1803 name 去重（config>builtins>MCP>ACP）/ #24 soft-load（MCP+ACP try/except）/ tool_search 延迟装配 fail-closed（`assemble_deferred_tools` + `DeferredToolCatalog` 逻辑与上游一致）/ host-bash 过滤（`is_host_bash_allowed`）/ view_image vision 门控。其余 builtin（update_agent/clarification/view_image/setup_agent/invoke_acp_agent/task_tool）差异**仅文档措辞 + 日志格式 + 类型注解**，无逻辑差。补 **2 项对齐**：① `present_files` 移植上游重写——单文件→**多文件**（`filepaths: list[str]`）+ 路径归一化（`_normalize_presented_filepath`，虚拟/宿主路径都解析到物理、校验落在 `outputs_path` 下、回写规范虚拟路径）+ **穿越校验**（新增 `Paths.resolve_virtual_path`，`relative_to(base)` 挡 `..`）+ `ToolMessage` 反馈（`InjectedToolCallId`）；② `sync.py` 修正 `functools.partial` 调用（旧 `inner=coro.func` 丢绑定参数→直接 `coro(*args)`，对齐上游，消除潜在 bug）。**#2676/#3665 task_tool 用户上下文**（`user_role`/`oauth_provider`/`oauth_id`）属 Gateway auth、设计上不 port（同 M11）。+ 14 项 hermetic 测试（present_file 多文件/路径校验/穿越/错误反馈 + `resolve_virtual_path` 合法+拒绝 + sync partial 保留）+ `docs/tools.md` 更新（M15 header + present_files 设计原理小节 + 逐文件「为什么单独」分析 + FAQ）。基线 1572 → **1586 passed**, 0 lint。详见 §3.1/§3.2（M15 行已 ✅）。

**M13 memory 六维重审（2026-06-27）**：7 文件逐个 diff 最新上游——确认 #2627（同步 LLM 路径，`asyncio.to_thread` + `model.invoke`，不创事件循环）/ #20（`user_id` 入队时捕获进 `ConversationContext`，跨 `threading.Timer` 不取 contextvar）/ #3429（`memory.token_counting: char` 配置 + tiktoken 懒加载 + 600s 失败 cooldown + CJK 感知 char 估算 fallback）/ #3592 correction 信号 prompt 文本 / per-user + per-agent 隔离（`(user_id, agent_name)` 缓存键）**mini 均已含**。补 **2 个 bug 对齐**：① **#3697** `filter_messages_for_memory` 加 `if msg.additional_kwargs.get("hide_from_ui"): continue`——中间件注入的隐藏消息（TodoMiddleware `todo_reminder`、ViewImageMiddleware、DynamicContextMiddleware `__memory` 载荷）不进记忆 LLM，防框架内部文本污染 + `__memory` 自放大循环；② **#3719** 空白/纯空白 fact 跳过——`_fact_content_key` 对 strip 后为空的 content 返回 None，旧版 `if fact_key is not None and fact_key in existing` 合并条件在 `fact_key is None` 时为 False、**不跳过**，空 fact 仍被 append 进记忆；拆成 `if fact_key is None: continue` + `if fact_key in existing: continue` 两步守卫。**#3592 guaranteed injection**（`guaranteed_categories`/`guaranteed_token_budget`，correction fact 在 token 压力下保底注入）跨 `prompt.py`+`memory_config`+`lead_agent/prompt` 3 模块、属新增特性非 bug，归后续专项（同 #3658 的处理）。+ 2 项 hermetic 测试（`test_filter_drops_hide_from_ui_messages` / `test_whitespace_only_fact_skipped`）+ `docs/memory.md` 更新（M13 header + hide_from_ui/空白 fact 设计原理 + 逐文件「为什么单独」+ FAQ）。基线 1586 → **1588 passed**, 0 lint。详见 §3.1/§3.2（M13 行已 ✅）。

**M14 skills 六维重审（2026-06-28）**：12 文件逐个 diff 最新上游（`__init__`/`installer`/`parser`/`validation`/`slash`/`types`/`security_scanner`/`tool_policy`/`permissions`/`storage/__init__`/`storage/skill_storage`/`storage/local_skill_storage`），剥 docstring 后**逻辑差异几乎为零**。五大关注点 **mini 均已含**：**#23** 安装防护（`is_unsafe_zip_member` 拒绝对路径/`..` + `is_symlink_member` 跳 symlink + `safe_extract_skill_archive` 512MB zip 炸弹上限 + 每成员 `is_relative_to(dest)` + macOS `__MACOSX`/dotfile 过滤）/ **#2626** allowed-tools 强制（`filter_tools_by_skill_allowed_tools`，None=legacy 全放行 / 空集=禁全部）/ **#3466** slash 加固（严格 `/name<ws|eol>` 正则 + `RESERVED_SLASH_SKILL_NAMES` 6 保留字 + 前导空白拒）/ **#2987** scanner JSON 容错（剥 markdown 围栏 + 花括号配平）/ per-user+public+custom 三布局（`load_skills` 扫 `public`+`custom` + extensions enabled 合并）。**#3335**（YAML 错误行号 + `mapping values are not allowed here` 引号提示）mini 也已含。补 **1 个并发 bug 对齐 #3778**：storage 单例生命周期——上游加了 `_skill_storage_lock = threading.Lock()` + 锁内双检构建（构建在锁内，镜像 `get_memory_storage()` 而非 sandbox_provider 的「锁外构造再丢败者」——因 `SkillStorage` 无 `teardown()` 钩子，败者无法清理）+ 锁内 `reset_skill_storage()`；mini 旧版**全裸**（并发冷启动会构出多份、reset 可在并发读当口清空全局）。`local_skill_storage.py` 的 `resolve_path` import（mini 走 `config.paths`、上游走 `config.runtime_paths`）是 mini 无 `runtime_paths.py` 的已知选择（§2.2 🟢低，独立任务），不 port。+ 5 项 hermetic 测试（`TestSkillStorageSingleton` 3 项：8 线程 barrier 并发冷启动恰好构 1 个 / 二次调用复用 / reset 后重建；`TestInstaller` +2：`is_symlink_member` 检测 + `safe_extract_skips_symlink_member` 跳过不解压）+ `docs/skills.md` 更新（M14 header + 数据流架构图「谁触发谁」+ 12 文件逐个「做什么/为什么单独」+ #3778 单例生命周期设计原理 + FAQ）。基线 1588 → **1593 passed**, 0 lint。详见 §3.1/§3.2（M14 行已 ✅）。

**M20 mcp + M21 community + M23 uploads 六维重审（2026-06-28）**：三模块逐文件 diff 最新上游，剥 docstring 后**仅 1 个 real bug**（已补），余者全为「mini 已含 / 重构差异 / additive 特性 defer / Gateway 不 port」。
- **M20 mcp（6 文件）**：确认 **#30** OAuth token 刷新双检锁（`async with lock`+re-check）/ **#3379** stdio 会话在 owning loop 关闭 + **#3089** 跨调用持久 + **#3203** HTTP/SSE 跳过池化（session_pool 完整架构：`owning_loop` 追踪 + 跨循环驱逐 + in-flight 去重 + `SESSION_CLOSE_TIMEOUT=5.0`）/ mtime 缓存失效 **均已含**；mini `cache.py` 用 `asyncio.get_running_loop()` 替代上游已废弃的 `get_event_loop()`（**Python 3.14 适配更优**）；session_pool 用 `Any` 而非 `ClientSession` 是软加载适配。补 **#3772 工具发现失败隔离**：旧版 `client.get_tools()` 一把梭（任一 server 发现失败 → 全部工具丢），改为按 server 独立 `get_tools(server_name=name)` + `asyncio.gather` + try/except（坏 server 只丢自己）。**#3597** stdio MCP 产物文件虚拟路径翻译（9 函数 ~370 行：workspace 快照 + 路径重写 + 文件名去重）归后续专项；**#3294** auth interceptor 透传 channel `user_id` 属 Gateway auth 不 port。
- **M21 community（12 provider）**：确认 **#3423** DDG Wikipedia region 推断（7 个 Unicode 码点范围）/ **#3418** jina reader proxy（`coerce_proxy`+`crawl(proxy=...,trust_env=...)`）/ 各 provider fail-closed **均已含**；差异全是 mini 抽 `_common.py` 共享 helper（`get_tool_extras` 返 `{}` 免判空 + `coerce_bool/timeout/proxy` + `normalize_search_result` + `truncate_content`）+ `%-format` 日志 + 中文注释。**defer 3 个 additive 特性**（非 fix，mini 已有 9 个搜索 provider）：**#3585 fastCRW**（firecrawl 薄封装变体）/ **#3675 GroundRoute**（纯 httpx、最易补）/ **#3575 Serper Google Images**（含其专属 SSRF 守卫 `_safe_public_url`）。
- **M23 uploads**：核心函数（`normalize_filename`/`claim_unique_filename`/`validate_path_traversal`/`list/delete/upload`）逐个对齐**零逻辑漂移**；**#3311** middleware 扫描 `run_in_executor` 卸载（拷 contextvar 保 `get_effective_user_id`）/ **#2623** 拒 symlink + **#2794** Windows 适配（mini 用 `open/write_upload_file_no_symlink`+`UnsafeUploadPathError`，**比上游 harness 层更完整**——上游等价在 Gateway `app/`）**均已含**。结构性差异：mini 转换放 `uploads/conversion.py`、上游放 `utils/file_conversion.py`（组织选择）；mini 多 `convert_with_pool` worker 池 + `*_no_symlink` 封装。**不 port**：上游 `get_uploads_dir`/`ensure_uploads_dir` 的 `user_id` kwarg（**#3579** IM channel owner-scoping，mini 无 `app/channels/`，HTTP/embedded 用 `get_effective_user_id()` 已正确）。
+ 2 项 hermetic 测试（`test_one_failing_server_does_not_block_others` / `test_all_servers_failing_returns_empty_not_raise`，锁住 #3772 隔离；fake `_FakeClient.get_tools` 加 `server_name` 支持 + `failing_servers` 旋钮）+ `docs/mcp.md`（M20 重审 header + #3772 数据流图设计原理）/ `docs/community.md`（M21 header + 12 provider 审计 + 3 defer）/ `docs/uploads.md`（M23 header + 核心对齐 + #3579 不 port 说明）更新。基线 1593 → **1595 passed**, 0 lint。详见 §3.1/§3.2（M20/M21/M23 行已 ✅）。

---

## 二、未完成内容提纲（与 deer-flow/backend 差距，按可做性排序）

> 逐模块全量对比 `deer-flow/backend` 的结论。**主线 agent 核心零差距**；差距的 ~90% 代码量是 mini 设计上不 port 的 Gateway 层。

### 2.1 真正可选（依赖独立外部包，按需）

| 项 | deer 代码 | 依赖 | 说明 |
|----|----------|------|------|
| **Guardrail 中间件** | `guardrails/`（4 文件 191 行：provider + builtin + middleware） | `guardrails` / `neMo-guardrails` | M16 第 7 步跳过位；Pre-tool-call 授权（Allowlist/OAP/自定义 provider） |
| **DeerFlowClient** | `client.py`（1327 行） | — | 嵌入式 in-process 客户端；mini 走 langgraph dev |

### 2.2 真实可用性缺口（建议补，按优先级）

| 优先级 | 项 | 缺什么 | 为什么补 |
|--------|----|--------|----------|
| ✅ 已补 | `models/vllm_provider.py` | `VllmChatModel`（子类化 ChatOpenAI，保留 vLLM `reasoning` 字段在 full response / streaming delta / follow-up tool-call turn） | **已补齐（2026-06-26）**：移植 `VllmChatModel`（逻辑 verbatim 自 deer-flow）+ 21 项 hermetic 测试 `test/test_vllm_provider.py` + `docs/models.md` §4.6/§5/§7.7/§9-Q7 + `config.example.yaml`「路径 D」推理模型示例。vLLM 推理模型配置现可用 |
| 🟡 中 | `models/patched_*.py`（5） | DeepSeek / MIMO / MiniMax / OpenAI / StepFun 的流式 + reasoning 字段 workaround | 原生 langchain 类能跑，但这些 provider 有兼容 rough edges；视用户用哪些 |
| 🟡 中 | `scripts/migrate_user_isolation.py` | legacy `memory.json`/`threads/`/`agents/` → per-user 布局迁移 | mini 已是 per-user（M13/M22），但用户**从 deer-flow 迁过来**时有用 |
| 🟢 低 | `config/tracing_config.py` | `TracingConfig` 单例 + `reset_tracing_config` + `get_enabled_providers` | mini tracing 走 env 直读够用；缺配置单例 + 重置能力（管理层 nice-to-have） |
| 🟢 低 | `config/runtime_paths.py` | `project_root` / `runtime_home`（`DEER_FLOW_HOME` env 覆盖） | mini 用 `config/paths.py`；缺 env 覆盖能力 |
| 🟢 低 | `models/claude_provider.py` / `mindie_provider.py` / `openai_codex_provider.py` / `credential_loader.py` / `assistant_payload_replay.py` | 额外 provider 适配 / 凭证加载 / payload 回放 | langchain 已有 `ChatAnthropic` 等；mini 经 env/`$VAR` 直读；低 |

### 2.3 设计上不 port（mini 是 harness 教学版，非全栈产品）

| 项 | 规模 | 性质 |
|----|------|------|
| **`app/` Gateway 层** | 61 .py（17 API 路由 + 13 文件 auth[JWT+密码+多 provider] + 16 文件 IM 渠道[feishu/slack/telegram/discord/dingtalk/wechat/wecom]） | FastAPI 应用层；mini 走 `langgraph dev` 或基于 `runtime_lifespan` bundle 自搭 |
| **连带持久化** | `persistence/channel_connections/` / `feedback/` / `user/`（各 3 文件） | Gateway 专属 |
| **连带 config** | `config/channel_connections_config.py` / `suggestions_config.py` / `agents_api_config.py` | Gateway 专属 |
| **DB 迁移** | `persistence/migrations/`（alembic） | mini 走 `create_all`（教学简化） |
| **顶层文件** | `Dockerfile` / `debug.py` / `sitecustomize.py` / `ruff.toml` | 部署 / 调试；mini 用 pyproject 内 ruff |
| **backend/docs/** | 30 篇运维文档（API/AUTH_DESIGN/IM_CHANNEL/STREAMING…） | 多数对应不做的 Gateway；少数产品化时可参考 |

> **若要做薄 Gateway demo**（~3 文件，~200 行）：`app/main.py`（挂 `runtime_lifespan` bundle）+ `app/routers/runs.py`（POST `/threads/{id}/runs/stream` SSE + `/wait`）+ `app/routers/threads.py`（列表 + DELETE）。**不碰** auth / IM channels / feedback / artifacts。仅当要把 mini 当产品底座时才做——否则用上游 deer-flow 更省事。

---

## 三、质量加固 — 全量重审（六维审查，触发：deer-flow 上游已更新）

> **🔴 触发（2026-06-26）**：`./deer-flow` 上游代码已更新（最近提交 2026-06-26，近 60 天 harness 大量改动）。mini 此前的「全面对标 v1.2」基线**已过时**——必须以**当前最新** deer-flow 为准，对**每个 Phase（0–8 全部）重新做质量审查**。Phase 1 + 7–8 之前的「零严重 bug」结论是针对**旧**上游的，上游已变，**结论须重新验证**（不止补文档，代码对齐也要重查）。

> **统一要求——六维审查，适用于全部 Phase（0–8）：**
>
> 1. **同步检查对应文档**。审查某个模块时，必须同时审 `docs/<module>.md`，**不能只审代码**。
> 2. **文档深度标准（面向小白，便于新人学习——这是核心目标）**：
>    - **面向小白**——每个名词第一次出现都要解释，不假设读者会 LangGraph / deer-flow；
>    - **逐文件分析**——讲清该模块下**每个代码文件的作用**（不是只贴一段代码片段），每个文件一段「它做什么、为什么单独成文件」；
>    - **代码架构**——画出 / 讲清**各模块下的代码架构**：文件关系、调用链、数据流、状态机，谁依赖谁、谁触发谁；
>    - **目标**：新人看完能复述「这个文件做什么、在模块哪一环、为什么这么拆」，并能据此上手改代码。
> 3. **上游同步对齐（新增第六维）**。逐文件 diff mini vs **当前** deer-flow，查三类漂移：
>    - ① **上游新增、mini 缺失**（新 middleware / 新 provider / 新 config / 新 agent feature）→ 评估是否补；
>    - ② **上游修复、mini 未跟进**（ID-swap / 单例生命周期 / loop detection / memory 过滤等红线 bug）→ 必须跟进；
>    - ③ **上游性能优化**（O(n)→O(1) 索引等）→ 可选跟进。
>    - 精确漂移清单见 §3.1，全 Phase 重审范围见 §3.2。
> 4. **已审 Phase 也要重做**。Phase 1（M4–M9）+ Phase 7–8（M17/M18/M19/集成）之前的结论针对旧上游；按新六维标准重审其对应
>    9 篇文档（persistence / checkpointer / run_event_store / run_journal / stream_bridge /
>    serialization / agents / runs / runtime_store + architecture）**并重查代码对齐**——「零严重 bug」不再假设成立。

### 3.1 上游漂移清单（2026-06-26 精确 diff，重审时逐一核对）

> 来源：`cd deer-flow && git log --since=2026-04-22` + 逐文件存在性核对。mini 整体模块面已较新（多数上游新增 mini 已有：dynamic_context / safety_finish_reason / tool_output_budget middleware、brave/browserless/searxng/serper provider、features.py / factory.py / summarization_hook 等）。**真正漂移在两类**：① mini 仍缺的少数文件；② 上游近 60 天的 bug/perf 修复（需逐文件 diff 确认 mini 是否跟进）。

**A. mini 仍缺的文件（评估是否补）**

| 类别 | mini 缺 | 上游用途 | 建议 |
|------|--------|---------|------|
| Middleware | `input_sanitization_middleware` | 输入净化（安全） | ✅ 已补（2026-06-27，#1） |
| Middleware | `system_message_coalescing_middleware`（#3711） | LLM 请求前合并 SystemMessage | ✅ 已补（2026-06-27，#20） |
| Middleware | `token_budget_middleware` | token 预算控制 | ✅ 已补（2026-06-27，#23 + token_budget_config） |
| Checkpointer | `agents/checkpointer/async_provider.py` | 异步 checkpointer provider | ✅ 已有（2026-06-29 Phase 1 核对：mini 早已有该文件且与上游 AST 级零漂移；§3.1-A 旧记「mini 缺」过时勘误） |
| Config | `config/auth_config.py` | 认证配置 | 设计上不 port（Gateway 专属） |
| Config | `config/runtime_paths.py` | `DEER_FLOW_HOME` env 覆盖 | §2.2 🟢低 已列 |
| Community | `fastcrw` / `groundroute` | 额外联网 provider | 视需求，低 |

**B. 上游近 60 天 bug 修复（逐文件 diff 确认 mini 是否跟进）**

| PR | 主题 | 影响模块 | mini 是否跟进 |
|----|------|---------|-------------|
| #3746 | ID-swap 递归注入 + orphan peer 压缩 | DanglingToolCall（M16） | ✅ 已含（逐文件 diff：仅文档措辞，无逻辑差） |
| #3730 | sandbox provider 单例生命周期同步（+ 并发回归） | sandbox（M10/M10b） | ✅ 已补（2026-06-29：`sandbox_provider.py` 加 `_provider_lock = threading.Lock()` 守卫 get/reset/shutdown/set 全部 4 个位点；get 快路径一次带锁读，冷启动在锁外构造后回锁下裁决[谁先装谁赢]，回调[`resolve_class`/`__init__`/`reset`/`shutdown`]在锁外跑避免自死锁，输家实例 `shutdown()` 防泄漏[issue #3721]；镜像 `get_memory_storage()` / M14 #3778） |
| #3778 | skill storage 单例生命周期同步 | skills（M14） | ✅ 已补（2026-06-28：`_skill_storage_lock = threading.Lock()` + 锁内双检构建 + 锁内 `reset_skill_storage()`；构建在锁内镜像 `get_memory_storage()`，因 `SkillStorage` 无 teardown 钩子） |
| #3772 | MCP tool 发现失败隔离 | mcp（M20） | ✅ 已补（2026-06-28：`get_mcp_tools` 把 `client.get_tools()` 一把梭改为按 server 独立 `get_tools(server_name=name)` + `asyncio.gather` + try/except，坏 server 只丢自己不拖累健康 server） |
| #3711 | SystemMessages 合并（coalesce） | M16（新 middleware，mini 缺） | ✅ 已补（system_message_coalescing_middleware #20） |
| #3709 | loop detection 位置回退 bug | LoopDetection（M16） | ✅ 已含（逐文件 diff：频率层 + 延迟 wrap_model_call 注入俱在） |
| #3697 | hide_from_ui HumanMessage 从 memory 过滤 | memory（M13） | ✅ 已补（2026-06-27 M13，见下重复行） |
| #3719 | 空白 fact 跳过 | memory updater（M13） | ✅ 已补（2026-06-27 M13，见下重复行） |
| #3729 | sandbox artifact 挂载对齐 channel user | sandbox（M10） | ⬜ 设计上不 port（2026-06-29：`acquire(*, user_id)` + 按 `(user_id, thread_id)` 归桶 + sandbox_id 嵌 user_id，属 IM channel owner-scoping——触及 `app/channels/feishu.py` + `app/gateway/routers/uploads.py`，同 task_tool #2676 / uploads #3579 / mcp #3294 不 port；mini 经 `get_effective_user_id()` 已正确按用户隔离路径，无 IM channel 故无 owner 覆盖需求） |
| #3732 | preserve langgraph resume command | runtime（M18） | ✅ 已对齐（2026-06-28：逐文件 diff worker/manager，均无吞 `Command(resume=...)` 的预处理——`graph_input` 原样传 `agent.astream`，resume 走 LangGraph 原生路径，worker 无需特判） |
| #3687 | 流式 AI 消息去重 O(n²)→O(n)（seen-id 集合） | subagents（M11） | ✅ 已补（2026-06-27：`seen_message_ids` 集合预建 + 循环内 O(1) 查 + append 后维护） |
| #3658 | 按**模型**归桶 token（`token_usage_by_model`） | subagents+journal+worker+persistence | ✅ 已补（2026-06-28：整体 port 跨 5 处——`token_collector.on_llm_end` 取 `response_metadata.model_name` 入记录 + `journal._record_model_usage` 分桶 `self._tokens_by_model` + `RunRecord.token_usage_by_model` 字段 + `RunStore.update_run_completion/progress` 签名 + `MemoryRunStore.aggregate_tokens_by_thread` 读侧按 `token_usage_by_model` 逐模型累加[老行回退 `model_name`]；mini 无 `persistence/run/{model,sql}.py` ORM 故 SQL schema/migration N/A，只在内存层 port） |
| #3559 | 子代理图 `checkpointer=False` 隔离父 run | subagents（M11） | ✅ 已含（逐文件 diff：`_create_agent` checkpointer=False + docstring） |
| #3611 | 子代理 span 归属父 thread 的 Langfuse session | subagents+tracing（M12） | ✅ 已补（2026-06-28：`subagents/executor.py::_aexecute` 图根挂 `build_tracing_callbacks`（#17）+ 注入 `inject_langfuse_metadata`——父 thread_id→`langfuse_session_id`、`task_tool` 经 `resolve_runtime_user_id` 捕获的 user_id→`langfuse_user_id`、`subagent:<归一化名>`→`langfuse_trace_name`；executor 加 `user_id` 参数跨隔离线程不丢；不 port 上游 client.py/worker 注入点[Gateway 层]） |
| #3370/#3342/#2885 | tool-search 加固（MCP 元数据 tag 合并 + 可靠隐藏 schema + promote 跨重入保留） | tools（M15） | ✅ 已含（逐文件 diff：`DeferredToolCatalog`/`assemble_deferred_tools` 逻辑与上游一致，无 ContextVar，差异仅注释） |
| —（无单 PR） | `present_files` 单文件→多文件 + 路径归一化 + 穿越校验 | tools（M15） | ✅ 已补（2026-06-27：移植上游重写 + 新增 `Paths.resolve_virtual_path`） |
| #2676/#3665 | task_tool 透传用户上下文（`user_role`/`oauth_provider`/`oauth_id`/`run_id`） | tools+subagents | ⬜ 设计上不 port（Gateway auth 概念；`user_id` mini 经 `get_effective_user_id` 已处理） |
| #3697 | `hide_from_ui` HumanMessage 从 memory builder 过滤 | memory（M13） | ✅ 已补（2026-06-27：`filter_messages_for_memory` 加 `additional_kwargs.hide_from_ui` 跳过） |
| #3719 | 空白/纯空白 fact 跳过 | memory updater（M13） | ✅ 已补（2026-06-27：拆 `if fact_key is None: continue` + `if fact_key in existing: continue` 两步守卫；旧合并条件漏跳空白 fact） |
| #3592 | correction fact 的 guaranteed 注入（`guaranteed_categories`/`guaranteed_token_budget`） | memory prompt+config+lead_agent | 🟡 归后续专项（跨 3 模块的新增特性非 bug；mini `format_memory_for_injection` 无保底通道） |
| #2627/#3429/#20 | 同步 LLM 路径 + token_counting 配置 + user_id 跨 Timer | memory（M13） | ✅ 已含（逐文件 diff：`asyncio.to_thread`+`model.invoke` / `memory.token_counting: char` + tiktoken 冷却 / `ConversationContext.user_id` 捕获） |

**C. 上游性能优化（可选跟进）**

| PR | 主题 | 影响模块 |
|----|------|---------|
| #3688 | AppConfig.get_*_config O(n)→O(1) 索引 | config（M0） | ✅ 已补（2026-06-28：`_models_by_name`/`_tools_by_name` PrivateAttr dict + `_build_name_indexes` model_validator(after)；`get_model_config`/`get_tool_config` 由 `for/next` 线性扫改 O(1) dict 查；`setdefault` 保重名首条；PrivateAttr 不进序列化；reload 构新 AppConfig 自动刷新） |
| #3686 | MemoryRunEventStore 按 run_id 索引 | run_event_store（M6） | ✅ 已补（2026-06-29：`MemoryRunEventStore` 加 `_events_by_run`/`_messages_by_run` run 分桶投影[同一 dict 对象、无拷贝]，`list_events`/`list_messages_by_run` 由扫整个 thread O(该 thread 事件数) 改 O(该 run 事件数)；`_put_one`/`delete_by_run`/`delete_by_thread` 同步维护；brute-force 等价性测试钉死语义） |
| #3562 | MemoryRunStore 按 thread_id 索引 | runs（M18） | ✅ 已补（2026-06-28：`MemoryRunStore` 加 `_runs_by_thread` 二级索引 + `_index_run`/`_unindex_run`，`put`/`delete` 锁下同步；`list_by_thread`/`aggregate_tokens_by_thread` 由全扫改 O(该 thread)） |
| #3700 | SSE resume offset O(1) | stream_bridge（M8） | ✅ 已补（2026-06-28：`_resolve_start_offset` 由线性扫缓冲改 `_parse_event_seq` 算术 O(1)，算出 index 仍核验 id；M8 完整重审归 Phase 1） |

**D. 已知结构性分歧（非 PR，待对齐上游）**

> M16 全维重审中确认、但**有意推迟到下次做**的 mini↔上游代码分歧——不是 §3.1-B 的 PR 修复，是**中间件链装配顺序**差异。已决：对齐上游。

| 项 | 现状（mini） | 上游 | 决定 | 影响（改什么） | 状态 |
|----|-------------|------|------|---------------|------|
| middlewares 链：`Uploads` 与 `ThreadData` 顺序 | `ThreadData` → `Uploads` → `Sandbox`（[`tool_error_handling_middleware.py:156-164`](../backend/packages/harness/deerflow/agents/middlewares/tool_error_handling_middleware.py) `_build_runtime_middlewares`） | `Uploads` → `ThreadData` → `Sandbox` | **对齐上游（Uploads 提前到 ThreadData 之前）** | ① 翻转 `_build_runtime_middlewares` 两行 + 更新行内注释（现 146-153）；② 回归测试 `test_thread_data_precedes_sandbox_and_uploads`（[test_middlewares.py:125-132](../test/test_middlewares.py)，断言 `_idx(ThreadData) < _idx(Uploads)`）须翻向为 `test_uploads_precedes_thread_data_and_sandbox`（断 `_idx(Uploads) < _idx(ThreadData)`）；③ `docs/middlewares.md` §4 顺序说明同步。**核实依据（翻转安全）**：`UploadsMiddleware` 从 `runtime.context` 取 thread_id、自解析 uploads_dir，**不读 thread_data state**，故两者无硬先后依赖 | ✅ 已完成（2026-06-28，见 §四 已完成） |

### 3.2 全 Phase 重审范围（代码对齐 + 文档深度，含 Phase 0 + 已审 Phase）

| 档 | Phase / 模块 | 代码对齐（vs 最新 deer） | 文档深度 | 重点（代码 + 文档） |
|----|-------------|------------------------|---------|---------------------|
| 🔴 高 | Phase 6: **M16 middlewares** | ✅ | ✅ | **2026-06-27 完成**：补 3 middleware（input_sanitization #1 / system_message_coalescing #20 / token_budget #23）+ token_budget_config + `is_dynamic_context_reminder` 对齐；链 23→26 步；#3746/#3709 确认已含；GraphBubbleUp（#15）/ Clarification 末位（#14）不变量复验通过；72 新测试 + docs 全重写。**遗留 1 项已闭环**：Uploads/ThreadData 顺序已对齐上游（2026-06-28，Uploads→ThreadData→Sandbox，见 §3.1-D / §四 已完成） |
| 🔴 高 | Phase 2: **M11 subagents** | ✅ | ✅ | **2026-06-27 完成**：逐文件 diff 确认 #34（持久隔离循环）/ #35（单 scheduler pool, MAX_CONCURRENT=3）/ 5 状态契约 / `checkpointer=False`（#3559）/ 协作取消 / 单 SystemMessage 合并（#2701）/ 超时终态原子（#2583）/ #3146 / #2414 / #3610 **mini 均已含**（公共同构，差异仅中英 docstring）；补 1 项性能 **#3687**（流式 AI 消息去重 seen-id 集合 O(n²)→O(n)）；**#3658**（按模型归桶 token）归 M18；+2 hermetic 测试 + docs 全重写（内部数据流图 + 状态机 + 9 文件逐个分析） |
| 🔴 高 | Phase 5: **M15 tools** | ✅ | ✅ | **2026-06-27 完成**：15 文件逐个 diff——五大关注点（#1803 name 去重 / #24 soft-load / tool_search fail-closed / host-bash 过滤 / view_image vision 门控）**mini 均已含**；其余 builtin 差异仅文档措辞+日志格式。补 2 项对齐：① `present_files` 移植上游重写（单文件→**多文件** + 路径归一化 + **穿越校验** + `ToolMessage` 反馈）+ 新增 `Paths.resolve_virtual_path`；② `sync.py` 修正 `functools.partial` 调用（旧 `inner=coro.func` 丢绑定参数→`coro(*args)`）；#2676/#3665 task_tool 用户上下文属 Gateway auth 不 port；+14 hermetic 测试 + docs 更新（present_files 设计原理 + 逐文件「为什么单独」+ FAQ） |
| 🟡 中 | Phase 3: **M13 memory** · Phase 4: M14 skills | ✅（M13+M14） | ✅（M13+M14） | **M13 2026-06-27 完成**：7 文件 diff 确认 #2627（同步 LLM）/ #20（user_id 跨 Timer）/ #3429（token_counting）/ per-user 隔离 **已含**；补 2 bug：**#3697**（`filter_messages_for_memory` 过滤 `hide_from_ui`）+ **#3719**（空白 fact 拆两步守卫，旧合并条件漏跳）；**#3592**（guaranteed 注入）归后续；+2 hermetic 测试 + docs 更新。**M14 2026-06-28 完成**：12 文件 diff 确认 #23（zip/symlink/穿越防御）/ #2626（allowed-tools）/ #3466（slash 加固）/ #2987（scanner JSON 容错）/ #3335（YAML 行号提示）/ 三布局 **均已含**；补 **#3778**（storage 单例 `_skill_storage_lock` + 锁内双检构建 + 锁内 reset，旧版全裸有并发竞态）；+5 hermetic 测试（单例并发 + symlink 跳过）+ docs 更新（数据流图 + 12 文件逐个分析 + #3778 设计原理 + FAQ） |
| 🟡 中 | Phase 5: M20 mcp · M21 community · Phase 5.5: M23 uploads | ✅ | ✅ | **2026-06-28 完成**：M20 mcp 6 文件 diff 确认 #30（OAuth 双检锁）/ #3379+#3089+#3203（session_pool 完整架构）/ mtime 缓存 **均已含**（mini `cache.py` 用 `get_running_loop` 适配 3.14 更优）；补 **#3772**（工具发现按 server 独立 gather+try/except，旧版一把梭任一坏 server 全丢）；#3597 stdio 虚拟路径翻译 defer，#3294 Gateway auth 不 port。M21 community 12 provider diff 确认 #3423（DDG region）/ #3418（jina proxy）/ fail-closed **均已含**（差异全为 `_common.py` 重构 + 日志措辞）；defer 3 个 additive（fastcrw/groundroute/#3575 Serper Images+SSRF）。M23 uploads 核心函数零漂移，#3311/#2623/#2794 **均已含**（mini `*_no_symlink` 比上游 harness 更完整）；#3579 `user_id` kwarg 是 IM channel 不 port。+2 hermetic（#3772 隔离）+ 3 篇 docs 重审 header |
| 🟡 中 | Phase 2: M12 tracing · Phase 7: M17 agents · M22 agents_config | ✅ | ✅ | **2026-06-28 完成**：M12 tracing 3 文件 diff 零逻辑漂移；补 **#17+#3611 子代理注入点**（`executor._aexecute` 图根挂 callbacks + 注入 langfuse 元数据[父 thread_id→session、捕获 user_id→user、`subagent:<名>`→trace_name]；executor 加 `user_id` 参数，`task_tool` 经 `resolve_runtime_user_id` 捕获传入）；不 port 上游 client.py/worker 注入点。M17 agents 7 文件 diff 差异全为 docstring+类型注解+结构移动（`build_middlewares` 拆到 `middlewares/__init__.py`）；补 **SDK 路径 TokenBudget 半挂**（`features.py` 加 `token_budget` 字段 + `factory.py` 加步骤 [13]，M16 给 lead 链加了但 SDK 漏；#3592 guaranteed injection 归后续）。M22 agents_config 零漂移（per-user/legacy/#3390/AGENT_NAME_PATTERN 均已含）；上游 `agents_api_config.py` Gateway 专属不 port。+11 hermetic 测试（8 tracing wiring + 3 SDK TokenBudget）+ 3 篇文档重审 header |
| 🟡 中 | Phase 8: M18 runs · M19 store · 集成 | ✅ | ✅ | **2026-06-28 完成**：M18 runs（manager/worker/schemas/naming/store/*）剥 docstring 后状态机不变量（busy 重试 #2 / orphan 恢复 #7 / shutdown drain #6 / 幂等 cancel / create_or_reject 消除 TOCTOU / rollback 快照 #5）**逻辑完全一致**；补 **#3562**（`MemoryRunStore` 补 `_runs_by_thread` 二级索引，`list_by_thread`/`aggregate_tokens_by_thread` 由 O(全部 run) 全扫改 O(该 thread)）、**#3658**（按模型归桶 token 跨 token_collector+journal+RunRecord+store+聚合读侧 5 处整体 port）、**#3697**（journal 抓首条 human 也过滤 `hide_from_ui`，M13 在 memory 侧补过 journal 侧漏）；**#3732**（resume command）已对齐——worker 直传 `graph_input` 不吞 `Command(resume=...)`；清理 worker 死函数 `_extract_human_message`。M19 store + 集成（lifespan/langgraph.json/config.example）diff 仅 cosmetic（绝对 vs 相对导入；`get_app_config().checkpointer` 直读 vs `ensure_config_loaded()`+`get_checkpointer_config()` 包装；mini 无 Gateway 故抽 `runtime_lifespan`/`RuntimeBundle`——有意结构差异非漂移），装配/drain 顺序与上游 gateway `langgraph_runtime` 语义一致。**#3700**（SSE resume offset O(1)）顺带补：`stream_bridge/memory.py::_resolve_start_offset` 由线性扫缓冲改 `_parse_event_seq` 算术 O(1)（事件 id `{ts}-{seq}` 内嵌 == 绝对 offset 的 seq），算出 index 仍核验 id（外来/畸形/过期回退最早，行为同旧扫）。+24 hermetic 测试（6 per-model journal + 1 hide_from_ui + 10 store index/aggregate + 3 token_collector model_name + 4 SSE O(1)）+ 3 篇文档重审 header（runs.md M18 + runtime_store.md M19 + architecture.md 集成；runs.md 加 #3658 端到端数据流 §6.1）。基线 1606 → **1630 passed**, 0 lint |
| 🟡 中 | Phase 0: M-build/M0 config/M1 utils/M2 reflection/M3 user_context | ✅ | ✅ | **2026-06-28 完成**：M0 config 补 **#3688**（`AppConfig.get_model_config`/`get_tool_config` 由 O(n) `for/next` 扫改 O(1) `PrivateAttr` dict + `_build_name_indexes` model_validator，`setdefault` 保重名首条，热路径[community 工具每次 2-3 次 / agent 构建数次]不再重复扫）；M2 reflection 对齐上游错误处理（路径畸形/属性不存在→`ImportError` 带示例、缺包 `_build_missing_dependency_hint` 用 `err.name` 抓间接依赖 + `langchain_google_genai` 提示、`expected_type` 支持元组、类型不符 `ValueError`）。M3 user_context **逐字等价**上游（仅注释中译）；M-build Makefile/pyproject 对齐（有意不 port gateway/migrate-rev/detect-blocking-io——依赖 Gateway/alembic/扫描脚本）。**defer**：`utils/messages.py::message_to_text`（上游合并 helper，mini 有 3 处内联等价[RunJournal/memory/tool_output_budget]，跨 M7/M13/M16 重构非 bug）/ `utils/readability.py::Article.to_message` + subprocess 提取器（M21 附加多模态）/ 各子配置 `load_xxx_from_dict` 单例层（M19 已认定有意不 port）。+10 hermetic（7 #3688 索引[建表/重名保首/PrivateAttr 不序列化/冷启动隔离] + 3 reflection 新错误路径，含更新 5 项现有 reflection 测试的异常类型契约）+ 顺带修 flaky `test_skills::TestSkillStorageSingleton::test_concurrent_cold_start`（全局 counter 改按构造线程 tid 过滤，排除前序测试遗留后台线程经 bypass 路径[`app_config=`/`skills_path=`]的无关构造；断言「测试线程之间至多构 1 个」+「全部同实例」）。4 篇文档：`config.md`（Phase 0 header + #3688 §5.7 设计原理 + §7 签名注）/ `utils.md`（Phase 0 header + defer 清单）/ `user_context.md`（Phase 0 header 逐字等价）/ `build.md`（Phase 0 header Makefile 对齐）。基线 1630 → **1640 passed**, 0 lint |
| 🟡 中 | Phase 1: M-models/M4–M9 持久化 | ✅ | ✅ | **2026-06-29 完成**：逐文件 diff M-models/M4-M9 vs 最新上游（剥 docstring 后判逻辑差），补 **6 项真实对齐**（均闭环）。① **#3686** `MemoryRunEventStore` 加 run 分桶投影让单 run 读 O(该 run 事件数)；② **serialization** Interrupt 分支 + 保留 `__interrupt__`（#3595 SDK 中断识别）+ values 模式走 `_for_api` 剥 base64；③ **#3658 SQL 层**——M18 误判「mini 无 ORM」漏 port SQL 侧，本轮发现 `persistence/run/{model,sql}.py` 实都在：补 `RunRow.token_usage_by_model` 列 + `RunRepository.{update_run_completion,update_run_progress}` 签名 + `aggregate_tokens_by_thread` 由 SQL `GROUP BY model_name` 改 Python 侧按 `token_usage_by_model` 逐模型累加（老行回退 model_name）；④ **engine** `asyncio.to_thread(os.makedirs)`（红线 #28）+ `PRAGMA busy_timeout=30000`（默认 5s 太短）；⑤ **base** `@cache _column_keys` 热路径优化；⑥ journal `None` 类型对齐。**勘误**：§3.1-A 旧记「mini 缺 checkpointer/async_provider.py」**过时**——mini 早已有且 AST 级零漂移。**有意不 port**：上游 `bootstrap.py`+alembic（mini 走 create_all）、Codex/MindIE provider（mini 不 ship）。**defer**：`message_to_text` 跨 M7/M13/M16 合并（Phase 0 已定）。+11 hermetic（5 #3686 等价性 + 3 serialization Interrupt/base64 + 3 #3658 SQL per-model）+ WAL PRAGMA 测试加 busy_timeout 断言。7 篇文档重审 header + #3686/serialization/#3658 设计原理。基线 1640 → **1651 passed**, 0 lint |
| 🟢 低 | Phase 2: M10 / M10b sandbox | ✅ | ✅ | **2026-06-29 完成**（§3.2 最后一行 → **§三 全 Phase 重审收口**）：逐文件 diff `sandbox/*` + `community/aio_sandbox/*` vs 最新上游（剥 docstring 后判逻辑差），补 **4 项真实对齐**。① **#3730** provider 单例生命周期加锁——`_provider_lock` 守卫 get/reset/shutdown/set 4 位点，回调在锁外跑避免自死锁，输家 `shutdown()` 防泄漏（镜像 `get_memory_storage` / M14 #3778）；② **search.py** `should_ignore_name` 由 O(n) fnmatch 循环改预计算 frozenset + 编译正则（os.walk 热路径）；③ **#3786** 沙箱审计放行合法 heredoc（`shlex.split` 失败 `return 'block'`→`pass`；高危在 shlex 前已查，属 M16，漂移清单漏列本轮发现补）；④ AIO ErrorObservation 恢复重试显式 `create_session`/`cleanup_session`（防 session 泄漏）。**defer**：**#3729**（acquire user_id + `(user_id,thread_id)` 归桶）属 IM channel owner-scoping（Gateway）不 port；`tools.py` ACP 路径/custom mounts/MCP allowed paths(=#3597) 是 additive；AIO soft-load（红线 #24）/ `%`-format 日志为 mini 已知选择。+7 hermetic（5 #3730 并发回归 + 2 #3786 heredoc）+ AIO fake 加 create/cleanup_session。2 篇文档重审 header + #3730/#3786/recovery-session 设计原理。基线 1651 → **1658 passed**, 0 lint |

**经验**：本轮不是「补文档」，而是「上游漂移后的重新对齐」——代码侧必须 diff 最新 deer-flow（重点查 §3.1-A/B），文档侧全部按面向小白 + 逐文件 + 架构图标准过一遍。建议优先做 🔴 高三件（M16 / M11 / M15），其中 **M16 漂移最大**（mini 缺 3 middleware + 多个 bug 修复待跟进）。

---

## 四、下次开工（单任务推进制）

> **工作流——每次只做一项，做完必回写：**
>
> 1. **一次一项**。本节「当前任务」是**唯一**在推进的事——不开多线、不并行下一个。做完一项才开始下一项。
> 2. **完成标准** = 三交付齐全：① 代码对齐修复（含 §3.1 漂移跟进）；② hermetic 测试（`test/test_<module>.py`）覆盖新逻辑/回归；③ `docs/<module>.md` 按 §三 标准（面向小白 + 逐文件 + 架构图）重写；④ `cd backend && make test && make lint` 全绿。
> 3. **完成后必回写本节**（强制）：把「当前任务」移入「已完成」、把「队列」第一项提为新的「当前任务」**并细化到可执行步骤**、更新基线计数、回 §3.2 把对应行标 ✅。
> 4. 全 Phase 范围见 §3.2；上游漂移清单见 §3.1——每个任务都要对照这两张表。

### ✅ 已完成（本轮流）

- **`models/vllm_provider.py`**（2026-06-26）——移植 `VllmChatModel`（保 vLLM `reasoning` 字段）+ 21 项 hermetic 测试 + `docs/models.md` §4.6/§7.7 + `config.example.yaml` 路径 D。基线 1477 → **1498 passed**, 0 lint。
- **M16 middlewares 六维重审**（2026-06-27）——补 3 middleware（input_sanitization #1 / system_message_coalescing #20 #3711 / token_budget #23）+ `token_budget_config.py` + AppConfig 接线 + `is_dynamic_context_reminder` 对齐（认 Human+System）；链 23→26 步；#3746/#3709 逐文件 diff 确认已含；72 新测试 + `docs/middlewares.md` 全重写（26 步链 + 数据流架构图 + 25 文件逐个分析）+ `config.example.yaml` token_budget 段。基线 1498 → **1570 passed**, 0 lint。
- **M11 subagents 六维重审**（2026-06-27）——9 文件逐个 diff 最新上游：公共同构，差异几乎全为中英 docstring；确认 mini 均已含 #34（持久隔离循环）/ #35（**单** scheduler pool, MAX_CONCURRENT=3，非双池）/ 5 状态契约（#3146）/ `checkpointer=False`（#3559）/ 协作取消 / 单 SystemMessage 合并（#2701）/ 超时终态原子（#2583）/ #2414 / #3610；补 1 项性能 #3687（流式 AI 消息去重 seen-id 集合 O(n²)→O(n)）；#3658（按模型归桶 token）归 M18；+2 hermetic 测试 + `docs/subagents.md` 全重写（内部数据流图 + 状态机 + 9 文件逐个分析）。基线 1570 → **1572 passed**, 0 lint。
- **M15 tools 六维重审**（2026-06-27）——15 文件逐个 diff：五大关注点（#1803 name 去重 / #24 soft-load / tool_search fail-closed / host-bash 过滤 / view_image vision 门控）均**已含**；补 2 项对齐：① `present_files` 移植上游重写（单→**多文件** + 路径归一化 + **穿越校验** + `ToolMessage`）+ 新增 `Paths.resolve_virtual_path`；② `sync.py` 修正 `functools.partial` 调用（`inner=coro.func` 丢绑定参数→`coro(*args)`）；#2676/#3665 task_tool 用户上下文属 Gateway auth 不 port；+14 hermetic 测试 + `docs/tools.md` 更新（present_files 设计原理 + 逐文件「为什么单独」+ FAQ）。基线 1572 → **1586 passed**, 0 lint。
- **M13 memory 六维重审**（2026-06-27）——7 文件逐个 diff：确认 #2627（同步 LLM `asyncio.to_thread`+`model.invoke`）/ #20（user_id 跨 Timer 捕获）/ #3429（token_counting + tiktoken 冷却）/ per-user 隔离均**已含**；补 2 bug：**#3697**（`filter_messages_for_memory` 过滤 `hide_from_ui`，防框架内部消息污染 + `__memory` 自放大）+ **#3719**（空白 fact 拆两步守卫，旧合并条件 `fact_key is not None and ...` 漏跳空白 fact）；#3592（guaranteed 注入）跨 3 模块归后续；+2 hermetic 测试 + `docs/memory.md` 更新（hide_from_ui/空白 fact 设计原理 + 逐文件分析 + FAQ）。基线 1586 → **1588 passed**, 0 lint。
- **M14 skills 六维重审**（2026-06-28）——12 文件逐个 diff：确认 #23（zip 炸弹/symlink/穿越防御）/ #2626（allowed-tools 强制）/ #3466（slash 加固）/ #2987（scanner JSON 容错）/ #3335（YAML 错误行号提示）/ per-user+public+custom 三布局 **均已含**（剥 docstring 后逻辑差异几乎为零）；补 **1 个并发 bug #3778**：storage 单例生命周期——`storage/__init__.py` 加 `_skill_storage_lock = threading.Lock()` + 锁内双检构建（构建在锁内，镜像 `get_memory_storage()`，因 `SkillStorage` 无 `teardown()` 钩子败者无法清理）+ 锁内 `reset_skill_storage()`，旧版全裸有并发竞态；`local_skill_storage.py` 的 `resolve_path` import（mini 走 `config.paths`、上游走 `config.runtime_paths`）是 mini 无 `runtime_paths.py` 的已知选择（§2.2 🟢低）；+5 hermetic 测试（`TestSkillStorageSingleton` 3 项：8 线程 barrier 并发冷启动恰好构 1 个 / 二次调用复用 / reset 后重建；`TestInstaller` +2：`is_symlink_member` 检测 + `safe_extract_skips_symlink_member` 跳过不解压）+ `docs/skills.md` 更新（M14 header + 数据流架构图「谁触发谁」+ 12 文件逐个「做什么/为什么单独」+ #3778 单例生命周期设计原理 + FAQ）。基线 1588 → **1593 passed**, 0 lint。
- **M20 mcp + M21 community + M23 uploads 六维重审**（2026-06-28）——三模块逐文件 diff 最新上游，剥 docstring 后**仅 1 个 real bug**（已补）。**M20 mcp（6 文件）**：确认 #30（OAuth token 刷新双检锁）/ #3379+#3089+#3203（session_pool 完整架构：owning_loop 追踪+跨循环驱逐+in-flight 去重+`SESSION_CLOSE_TIMEOUT`）/ mtime 缓存失效 **均已含**（mini `cache.py` 用 `get_running_loop` 替代上游已废弃 `get_event_loop`，**3.14 适配更优**；session_pool `Any` 而非 `ClientSession` 是软加载适配）；补 **#3772 工具发现失败隔离**——旧版 `client.get_tools()` 一把梭（任一 server 发现失败→全部工具丢）改为按 server 独立 `get_tools(server_name=name)`+`asyncio.gather`+try/except（坏 server 只丢自己）；#3597 stdio 产物虚拟路径翻译（9 函数 ~370 行）defer，#3294 Gateway auth 不 port。**M21 community（12 provider）**：确认 #3423（DDG Wikipedia region 7 码点范围）/ #3418（jina proxy）/ fail-closed **均已含**（差异全为 mini `_common.py` 共享 helper 重构 + 日志措辞）；defer 3 个 additive（fastcrw/groundroute/#3575 Serper Images+SSRF 守卫，mini 已有 9 搜索 provider）。**M23 uploads**：核心函数零漂移；#3311（`run_in_executor` 卸载）/ #2623+#2794（symlink 防御，mini `open/write_upload_file_no_symlink`+`UnsafeUploadPathError` **比上游 harness 层更完整**）**均已含**；#3579 `user_id` kwarg 是 IM channel owner-scoping 不 port。+2 hermetic 测试（#3772 隔离：单坏 server 不拖累其它 / 全失败返空不抛；fake `_FakeClient.get_tools` 加 `server_name` 支持 + `failing_servers` 旋钮）+ `docs/mcp.md`（M20 header + #3772 数据流图）/ `docs/community.md`（M21 header + 12 provider 审计 + 3 defer）/ `docs/uploads.md`（M23 header + 核心对齐 + #3579 不 port）更新。基线 1593 → **1595 passed**, 0 lint。
- **【小修】middlewares 链 Uploads/ThreadData 顺序对齐上游**（2026-06-28）——翻转 `_build_runtime_middlewares`（[tool_error_handling_middleware.py](../backend/packages/harness/deerflow/agents/middlewares/tool_error_handling_middleware.py)）：旧 `ThreadData→Uploads→Sandbox` → 新 `Uploads→ThreadData→Sandbox`（对齐上游 `build_lead_runtime_middlewares`，上游用 `middlewares.insert(2, UploadsMiddleware())`）；更新行内注释 + 模块 docstring。回归测试翻向：`test_thread_data_precedes_sandbox_and_uploads`（断 `_idx(ThreadData) < _idx(Uploads)`）→ `test_uploads_precedes_thread_data_and_sandbox`（断 `_idx(Uploads) < _idx(ThreadData) < sandbox_i`）；测试 docstring + 默认链成员清单顺序同步。`docs/middlewares.md` 同步：§3 数据流架构图（before_agent 队列里 Uploads 在 ThreadData 前）、§4 节标题（`Uploads(#3)→ThreadData(#4)→Sandbox(#5)`）、§4 设计原理（删「两序皆可，本链按 ThreadData 在前」，改为「本链按上游顺序 Uploads 在前；Sandbox 仍须在两者之后」）、§8「顺序是契约」。**翻转安全依据**：`UploadsMiddleware` 从 `runtime.context` 取 thread_id、自解析 uploads_dir，**不读 thread_data state**，两者无硬先后依赖。测试数不变（rename），基线 **1595 passed**, 0 lint。详见 §3.1-D（行已 ✅）。
- **M12 tracing + M17 agents + M22 agents_config 全维重审**（2026-06-28）——三模块逐文件 diff 最新上游（剥 docstring 后判逻辑差），补 **2 项真实漂移**（均半挂，已闭环），余者全为「mini 已含 / cosmetic / deferred / Gateway 不 port」。
  - **M12 tracing**（3 文件 + 消费点）：`tracing/{__init__,factory,metadata}.py` 剥 docstring 后**零逻辑漂移**（仅注释翻译）。**#17 + #3611 子代理注入点缺失**——上游在 `subagents/executor.py::_aexecute` 图根也挂 `build_tracing_callbacks`（#17）并注入 `inject_langfuse_metadata`（#3611，子代理 trace 归属父 thread），mini 此前完全没有。补齐：`executor.py` 加 `import os` + `from deerflow.tracing import build_tracing_callbacks, inject_langfuse_metadata` + `_aexecute` 在 run_config 后挂 callbacks（在 collector 之后）+ 归一化子代理名（`subagent:<name.strip().lower().replace('_','-')>`）+ 注入 langfuse 元数据（父 thread_id→session、捕获 user_id→user、`subagent:<name>`→trace_name）；`executor.__init__` 加 `user_id` 参数（`task_tool` 经 `resolve_runtime_user_id(runtime)` 捕获传入，跨隔离 daemon 线程不丢——同 memory #20 思路）；`task_tool.py` 加 `resolve_runtime_user_id` import + 捕获 user_id 进 executor_kwargs。**不 port**：上游 `client.py`/`runtime/runs/worker.py` 注入点（Gateway/嵌入式客户端，§2.1/§2.3）。
  - **M17 agents**（7 文件）：差异**几乎全是中英 docstring + 类型注解引号 + mini 把 `build_middlewares` 从 `lead_agent/agent.py` 拆到 `middlewares/__init__.py` 的结构选择**，剥 docstring 后逻辑零漂移。`thread_state` reducers（`merge_sandbox` fail-closed / `merge_promoted` catalog_hash scope / `merge_viewed_images` 空 dict 清空）逻辑一致。补 **1 项半挂对齐**：SDK 路径 `create_deerflow_agent` 缺 TokenBudget——M16 给 lead 链（步骤 #23）加了 `TokenBudgetMiddleware`，但 SDK `RuntimeFeatures` 漏了 `token_budget` 字段、`factory.py` 漏了步骤 [13]。补齐：`features.py` 加 `token_budget: bool | AgentMiddleware = False` + `factory.py` 加步骤 [13]（Clarification 顺延 [14]）+ 链序 docstring 同步。deferred：**#3592 guaranteed memory injection**（跨 prompt.py+memory_config+lead_agent/prompt 3 模块的新增特性，非 bug）。
  - **M22 agents_config**（`config/agents_config.py`）：剥 docstring 后**零逻辑漂移**。五大关注点 **mini 均已含**：per-user 布局 / legacy 只读回退 / #3390（`resolve_agent_dir` 要 `config.yaml` 才认）/ AGENT_NAME_PATTERN 校验（红线 #32）/ 名称小写归一。上游多的 `config/agents_api_config.py` 是 Gateway 专属不 port。
  - + 11 项 hermetic 测试：`test_subagents.py::TestSubagentTracingWiring`（8 项——user_id 存储 / #17 callbacks 在 collector 后追加 / 空 callbacks 不报错 / #3611 字段映射 / 名字归一化 3 case / 空名退 `subagent` / DEER_FLOW_ENV tag / ENVIRONMENT 回退；打桩 `_build_initial_state`+`_create_agent` 短路，在 astream 捕获 run_config）+ `test_agent_with_middlewares.py`（3 项——token_budget=True 进链且在 Clarification 前 / 默认 False 不误装 / 自定义实例直用）。`test_agents_config.py` 已 562 行 8 类全场景（per-user/legacy/#3390/隔离），无需添加。3 篇文档更新：`docs/tracing.md`（M12 重审 header + 三个注入点表 + #3611 子代理归属设计原理 + 子代理名归一化/user_id 跨线程捕获 + FAQ）/ `docs/agents.md`（M17 重审 header + RuntimeFeatures 加 token_budget + 步骤 14→15 + 子代理 #3611 交叉链接）/ `docs/agents_config.md`（M22 重审 header + 「待落地」→「已落地」）。基线 1595 → **1606 passed**, 0 lint。详见 §3.2（M12/M17/M22 行已 ✅）/ §3.1-B（#3611 行已跟进）。
- **M18 runs + M19 store + 集成 全维重审**（2026-06-28）——「让 agent 真正跑起来」最后一环。逐文件 diff 最新上游（剥 docstring 后判逻辑差），补 **4 项真实对齐**（均闭环），余者全为「mini 已含 / cosmetic / 有意结构差异」。
  - **M18 runs**（`runtime/runs/{manager,worker,schemas,naming,store/*}`）：剥 docstring 后状态机不变量（busy 重试红线 #2 / orphan 恢复 #7 / shutdown drain #6 / 幂等 cancel / `create_or_reject` 消除 TOCTOU / rollback 快照 #5）**逻辑完全一致**（差异全为注释翻译）。补 **3 项对齐**：① **#3562**——`MemoryRunStore` 补 `_runs_by_thread` 二级索引（`RunManager` 早已有，但持久化后端 `MemoryRunStore` 缺；加 `_index_run`/`_unindex_run`，`put`/`delete` 锁下同步，`list_by_thread`/`aggregate_tokens_by_thread` 由 O(全部 run) 全扫改 O(该 thread 的 run 数)）；② **#3658**——按模型归桶 token（见下「#3658 数据流」整体 port）；③ **#3697**——`journal` 抓首条 human 时也过滤 `hide_from_ui`（M13 在 memory 侧补过，journal 侧 `on_chat_model_start` 漏）。**#3732**（preserve resume command）已对齐——逐文件 diff 确认 worker/manager 均无吞 `Command(resume=...)` 的预处理，`graph_input` 原样传 `agent.astream`，resume 走 LangGraph 原生路径，worker 无需特判。清理 worker 死函数 `_extract_human_message`（定义未调用，上游无，连带 revert 仅服务其注解的 `TYPE_CHECKING` import）。
  - **#3658 按模型归桶 token（跨 5 处整体 port）**：① 写侧 `subagents/token_collector.on_llm_end` 从 `gen.message.response_metadata` 取 `model_name`（`model_name` 主、`model` 备）入记录 + snapshot_records 返回类型带 `model_name`；② `journal` 加 `self._tokens_by_model` 累加器 + `_record_model_usage(model_name, in, out, total)`（total≤0 跳、None→"unknown" 桶），`on_llm_end` + `record_external_llm_usage_records`（子代理 token 回灌）双路调它，`get_completion_data` 返 `token_usage_by_model`（深拷贝）；③ `RunRecord` 加 `token_usage_by_model` 字段 + `_row_to_record` 读它；④ `RunStore.update_run_completion/progress` 签名加该参数；⑤ 读侧 `MemoryRunStore.aggregate_tokens_by_thread` 走 #3562 索引 + 按 `token_usage_by_model` 逐模型累加（老行无该字段→回退 `model_name`）。记 `response_metadata.model_name` 而非 config 里的 model——一个 agent 可能路由多模型（主+兜底/子代理换模型），真计费模型由 provider 返回决定。mini 无 `persistence/run/{model,sql}.py` ORM（用内存 dict），故 SQL schema/migration N/A，只在内存层 port。
  - **M19 store + 集成**：`runtime/store/{__init__,provider,async_provider,_sqlite_utils}.py` 剥 docstring后**逻辑完全一致**（三入口 / 双检锁单例 / soft-load 红线 #24 / None→InMemoryStore 红线 #25 / 共用 `_sqlite_utils`），差异仅 cosmetic（mini 绝对导入 vs 上游相对导入；mini `get_app_config().checkpointer` 直读 vs 上游 `ensure_config_loaded()`+`get_checkpointer_config()` 包装——lifespan 进时配置已加载，无需二次 ensure）。集成 `runtime/lifespan.py`（`runtime_lifespan`+`RuntimeBundle`）：上游无对应独立文件（装配散在 Gateway `app/gateway/deps.py::langgraph_runtime`，FastAPI 绑定），mini 无 Gateway 故抽成框架级 CM（CLI/测试/未来 Gateway 复用）——**有意结构差异非漂移**；装配序（engine→checkpointer/store/stream_bridge 并行→run_store/event_store/thread_store→RunManager+reconcile）与关停序（先 `shutdown()` drain 再关 checkpointer，红线 #6/#3373）经逐行核对与上游 `langgraph_runtime` **语义一致**。无需补丁。
  - **#3700 SSE resume offset O(1)**（顺带补，M8 域但本轮 重审范围 列入）：`stream_bridge/memory.py::_resolve_start_offset` 由 `for index,entry in enumerate(stream.events)` 线性扫（O(n)）改为 `_parse_event_seq` 解析事件 id `{ts}-{seq}` 内嵌的 per-run seq（== 绝对 offset），算术 `local_index = seq - start_offset` 定位（O(1)）；算出 index 处仍核验 `events[local_index].id == last_event_id`，外来/畸形/过期 id 回退到最早（行为同旧扫）。mini 早已有 `events` 字段 + 同款 `_next_id`，故只动 resolve 一处。**M8 完整重审（stream_bridge.md 文档 + 余项）归 Phase 1**。
  - + 24 项 hermetic 测试：`test_journal.py::TestPerModelTokenBucketing`（6——response_metadata.model_name 归桶 / model 键 fallback / 多模型分别累加 / 无 metadata 入 unknown 桶 / external records 按模型 / completion_data 返拷贝）+ `test_hide_from_ui_human_ignored`（TestFirstHumanExtraction）+ `test_run_manager.py::TestMemoryRunStoreIndex`（10——put 索引 / delete 反索引 / list_by_thread 走索引非全扫 / 未知 thread 返空 / stale 索引条目丢弃 / aggregate 按 token_usage_by_model 桶 / 老行回退 model_name / 新老行混合 / 跳非终态 / update_run_completion 持久化 token_usage_by_model）+ `test_subagents.py::TestTokenCollector`（3——从 response_metadata 取 model_name / model 键 fallback / 无 metadata 时 None）+ `test_stream_bridge.py::TestResumeOffsetO1`（4——`_parse_event_seq` 取 offset / 畸形返 None / resume 用内嵌 seq 算术 / 外来 plausible-seq id 回退）。3 篇文档更新：`docs/runs.md`（M18 重审 header + #3658 端到端数据流 §6.1 + #3732 对齐注 + §5 MemoryRunStore 索引注 + §6 journal 行标 per-model）/ `docs/runtime_store.md`（M19 重审 header——cosmetic 差异逐项）/ `docs/architecture.md`（集成 重审 header——mini 抽 lifespan 是有意结构差异 + 装配/drain 序对齐上游 gateway）。基线 1606 → **1630 passed**, 0 lint。详见 §3.2（M18/M19/集成 行已 ✅）/ §3.1-B（#3732/#3658 行已跟进）/ §3.1-C（#3562/#3700 行已跟进）。
- **Phase 0（M-build / M0 config / M1 utils / M2 reflection / M3 user_context）全维重审**（2026-06-28）——收口**地基**：config 驱动一切单件、utils 是全模块复用基础、user_context 的 `get_effective_user_id`/`resolve_runtime_user_id` 已被 M11/M12/M18 消费。逐文件 diff 最新上游（剥 docstring 后判逻辑差），补 **2 项真实漂移**（均闭环），余者全为「mini 已含 / 有意结构选择 / 附加 defer」。
  - **M0 config**：补 **#3688**——`AppConfig.get_model_config` / `get_tool_config` 旧版是 O(n) 线性扫（`for m in self.models` / `next(t for t in self.tools if ...)`），上游已用 `PrivateAttr` dict + `_build_name_indexes` model_validator 改 O(1)。这两个 getter 在热路径（`get_tool_config` 每个 community 工具[web_search/web_fetch/image_search]每次调用被 `_common.py::get_tool_extras` 读 2-3 次取 api_key/超时/proxy；`get_model_config` 每次 agent 构建 + 每个绑工具中间件）。port：加 `_models_by_name` / `_tools_by_name` 两个 `PrivateAttr(default_factory=dict)` + `@model_validator(mode="after") _build_name_indexes`（`setdefault` 保重名首条，与旧 `next(...)` 首匹配语义一致；PrivateAttr 不进 `model_dump` 序列化；reload 构新 AppConfig 时索引自动刷新）+ 两个 getter 改 `self._<x>_by_name.get(name)`。保留 mini 的 `get_model_config(name: str | None)`——None→首个模型（所有调用方都在外 `if model_name` 守卫，None 路径无害）。
  - **M2 reflection**：`resolver.py` 对齐上游 `resolvers.py` 错误处理——旧版对「路径无 `:`」「属性不存在」「类型不符」分别抛晦涩的 ValueError/AttributeError/TypeError；port 后三种「拿不到想要的符号」的失败**全部归一成 `ImportError`** 并附可操作提示：① 路径畸形（无 `:`）→ `ImportError` 带示例；② 缺包 → `_build_missing_dependency_hint` 用 `ModuleNotFoundError.name` 抓**真正缺失的模块**（可能是间接依赖如 `langchain_google_genai` 触发的 `google`），已知 provider 根模块即使间接缺失也优先给 provider 包提示，未知模块用 `_`→`-` 推导包名兜底（「总给提示」）；③ 属性不存在 → `ImportError`；④ `expected_type` 校验改 `ValueError` + 支持 `tuple[type, ...]` 元组。加 `langchain_google_genai` 到 hint 表。调用方（factory/tools/sandbox/skills/mcp/safety_finish_reason）均不 catch 特定异常类型，port 安全。
  - **M3 user_context**：与上游**逐字等价**（`CurrentUser` Protocol / `_current_user` ContextVar / set/get/reset/require / `DEFAULT_USER_ID` / `get_effective_user_id` / `resolve_runtime_user_id`[runtime.context→contextvar→default 三级] / `_AutoSentinel`+`AUTO`+`resolve_user_id` 三态）——差异仅注释中译。M11/M12/M18 已多处验证可用。
  - **M-build**：`Makefile` 6 target（install/dev/test/test-blocking-io/lint/format）与上游命名语义对齐；有意不 port 的上游 target（`dev`/`gateway` 跑 uvicorn Gateway、`migrate-rev` alembic、`detect-blocking-io` 扫描脚本+运行时 gate）均依赖 mini 不做的层。`pyproject.toml` ruff 配置（line-length=240/双引号/3.12+）一致。
  - **defer（精记）**：① `utils/messages.py::message_to_text`——上游新增的「整条消息→文本」合并 helper（handles BaseMessage 或 dict-shaped），mini 有 **3 处内联等价**（`RunJournal._message_text` / `memory.message_processing.extract_message_text` / `tool_output_budget_middleware._message_text`），合并是跨 M7/M13/M16 三模块的重构（join 语义还略有差异），非 bug，归后续专项；② `utils/readability.py::Article.to_message`（多模态 image_url 块）+ subprocess Readability.js 提取器——M21 community 附加特性（mini 走 import readabilipy + `_fallback_extract`，被 jina/browserless/infoquest 用），归 M21；③ `utils/file_conversion.py` 位置（mini 在 `uploads/conversion.py`，M23 已记）；④ 各子配置 `load_xxx_from_dict` 单例层（M19 已认定有意不 port）。`time.py`/`messages.py` 核心/`network.py`（`PortAllocator`）AST 级零漂移；`model_config`/`checkpointer_config` 等子配置字段默认完全等价（mini 用属性+docstring，上游用 `Field(description=)`）。
  - + 10 项 hermetic 测试：`test_config.py::TestNameIndexes`（7——model/tool 索引建表后命中 / 非 dict 无 name 条目跳过 / 重名保首[模型+工具] / 空配置空索引 / PrivateAttr 不序列化 / 新构实例不继承旧索引[reload 场景]）+ `test_reflection.py`（更新 5 项现有测试的异常类型契约[ValueError→ImportError、TypeError→ValueError、AttributeError→ImportError] + 新增 3 项：google provider 提示 / 间接依赖仍指 provider 包 / `expected_type` 元组）。顺带修 flaky **`test_skills.py::TestSkillStorageSingleton::test_concurrent_cold_start`**——全量套件下偶发 counter==2（前序测试[如 run_manager worker]遗留后台线程经 `lead_agent/prompt.py::get_or_new_skill_storage(app_config=...)` **bypass 单例路径**在测试窗口内构造 _CountingStorage），改全局 counter 为**按构造线程 tid 过滤**（只数本测试 8 线程的构造），断言「测试线程之间至多构 1 个」+「全部同实例」（真正的锁不变量），`test_second_call`/`test_reset` 同改按主线程 tid 数。4 篇文档：`docs/config.md`（Phase 0 header + #3688 §5.7 O(1) 索引设计原理[PrivateAttr/setdefault/model_validator after/reload 刷新] + §7 签名注）/ `docs/utils.md`（Phase 0 header + 4 项 defer 清单）/ `docs/user_context.md`（Phase 0 header 逐字等价）/ `docs/build.md`（Phase 0 header Makefile 对齐）。基线 1630 → **1640 passed**, 0 lint。详见 §3.2（Phase 0 行已 ✅）/ §3.1-C（#3688 行已跟进）。
- **Phase 1（M-models / M4–M9 持久化）全维重审**（2026-06-29）——「模型 + 持久化地基」收口。逐文件 diff M-models/M4-M9 vs 最新上游（剥 docstring 后判逻辑差），补 **6 项真实对齐**（均闭环），余者全为「mini 已含 / cosmetic / 有意结构差异 /勘误」。
  - **M6 #3686**：`MemoryRunEventStore` 加 run 分桶投影 `_events_by_run` / `_messages_by_run`（同一 dict 对象、无拷贝、按 seq 排序）。`list_events` / `list_messages_by_run` 旧版扫整个 thread 的事件日志（O(该 thread 的事件数)），改读 run 分桶（O(该 run 的事件数)）——一个 thread 累积成百上千 run 但单次请求只关心其中一个；`list_messages_by_run` 在分桶上用 bisect。`_put_one` / `delete_by_run` / `delete_by_thread` 同步维护投影（lockstep）。**语义不变**——brute-force 等价性测试（交错 trace、两游标同给、delete 后 upkeep）钉死。
  - **M9 serialization**（3 处联动）：① `serialize_lc_object` 加 **LangGraph `Interrupt` 分支**——`Interrupt` 是 `__slots__` 类（无 model_dump/dict/__dict__），否则落 str() 兜底产畸形 payload，现规范化 `{"value":..., "id":...}`；② `serialize_channel_values` **不再剥 `__interrupt__`**（#3595：SDK 据此识别中断事件），只剥 `__pregel_*`，其值由 ① 规范化；③ `serialize(mode="values")` 改用 `serialize_channel_values_for_api`——values 快照流给前端须像 REST 端点一样剥 hide_from_ui 的 base64 图片（之前漏剥）。
  - **M4 #3658 SQL 层（M18 漏 port 补齐）**：M18 误判「mini 无 persistence/run/{model,sql}.py ORM」只在内存层 port 了 #3658；本轮重审发现两文件**实都在**。补：`RunRow.token_usage_by_model` JSON 列（`server_default=text("'{}'")` 免回填）+ `RunRepository.{update_run_completion,update_run_progress}` 签名加该参数 + `aggregate_tokens_by_thread` 由 SQL `GROUP BY model_name`（假设一行一模型，错）改 Python 侧按 `token_usage_by_model` 逐模型累加（一个 run 可贡献多模型桶；老行回退 model_name）。
  - **M4 engine**：`asyncio.to_thread(os.makedirs)` 卸载（红线 #28——init_engine 跑在 async lifespan）+ `PRAGMA busy_timeout=30000`（Python sqlite3 驱动默认 5s 太短，并发启动 / 多 worker 误报 locked）；**有意保留 `create_all`**（不 port 上游 `bootstrap.py`+alembic，mini 教学简化）。
  - **M4 base**：`@cache _column_keys` 让 `to_dict()`/`__repr__()` 不每次 SQLAlchemy 内省（list 端点热路径）。
  - **M7 journal**：minor 对齐——`record_external_llm_usage_records` 形参类型加 `None`（model_name 可 None）+ docstring 补 model_name + `_counted_external_source_ids.add` 顺序对齐（功能等价）。M18 已 port 的 #3658/#3697 经核对与上游一致。
  - **勘误 §3.1-A**：旧记「mini 缺 checkpointer/async_provider.py」**过时**——mini 早已有且 AST 级零漂移。`checkpointer/provider.py` 差异是 M19 记过的有意选择（mini `get_app_config().checkpointer` 直读 vs 上游 `ensure_config_loaded()` 包装；lifespan 进时配置已加载，语义等价）。
  - **M-models factory**：无真实漂移——差异全为 cosmetic（import/文案）+ mini 防御性增强（`_maybe_build_tracing_callbacks` try/except、`get_default_model`、空 models 友好报错）+ 上游对 Codex/MindIE provider 的特殊处理（mini 有意不 ship，port 了 ImportError）。`vllm_provider.py` 已与上游一致。
  - **defer**：`utils/messages.py::message_to_text` 合并（跨 M7/M13/M16 三模块内联等价，join 语义略异，非 bug）——Phase 0 已定归附加专项。
  - + 11 项 hermetic 测试：`test_events.py::TestRunEventStoreByRunIndex`（5——list_messages_by_run 跨游标等价 / list_events 带过滤等价 / 分桶分区不变量 / delete_by_run 后 lockstep / delete_by_thread 清索引）+ `test_serialization.py`（3——Interrupt→{value,id} / Interrupt 默认 id / values 模式剥 base64；更新 2 项现有 channel_values 测试反映「保留 __interrupt__」）+ `test_persistence.py`（3——#3658 SQL per-model 归桶 / completion 持久化列 / progress 持久化列；WAL PRAGMA 测试加 busy_timeout 断言）。7 篇文档：`run_event_store.md`（#3686 §4.8 run 分桶投影设计原理）/ `serialization.md`（Interrupt §4.4 + __interrupt__ §4.2 + values 剥 base64）/ `persistence.md`（#3658 SQL §4.9 + busy_timeout §3.5 + to_thread/@cache §4.10）/ `run_journal.md` / `checkpointer.md`（勘误 async_provider）/ `stream_bridge.md`（#3700 §4.4 O(1) 算术补文档化）/ `models.md`。基线 1640 → **1651 passed**, 0 lint。详见 §3.2（Phase 1 行已 ✅）/ §3.1-C（#3686 行已跟进）/ §3.1-A（async_provider 行勘误）。

- **M10 / M10b sandbox 全维重审**（2026-06-29）——§三 全 Phase 重审的**收尾**（§3.2 最后一行）。逐文件 diff `sandbox/*` + `community/aio_sandbox/*` vs 最新上游（剥 docstring 后判逻辑差），补 **4 项真实对齐**（均闭环）。
  - **#3730 provider 单例生命周期加锁**：`sandbox_provider.py` 加 `_provider_lock = threading.Lock()` 守卫 `get`/`reset`/`shutdown`/`set` 全部 4 个位点。旧版裸 check-then-create 在多 OS 线程（主循环 + IM channel 线程）下双重初始化，被覆盖的 `AioSandboxProvider` 泄漏 idle-checker 线程；`reset`/`shutdown`/`set` 裸触全局与 in-flight `get` 竞争会返回 None/半成品。修法：get 快路径一次带锁读，冷启动在锁外构造后回锁下裁决（谁先装谁赢），**回调（`resolve_class`/`__init__`/`reset`/`shutdown`）在锁外跑**避免自死锁 + 慢拆除挡 get，输家实例锁外 `shutdown()` 防泄漏（issue #3721）。镜像 `get_memory_storage()` / M14 #3778。
  - **search.py `should_ignore_name` 性能**：由 O(n) `fnmatch` 循环改预计算 `_EXACT_IGNORE_NAMES`（frozenset，O(1)）+ `_GLOB_IGNORE_RE`（编译正则，一次 match）。os.walk 每个目录/文件都调它，是搜索热路径。
  - **#3786 沙箱审计放行合法 heredoc**（属 M16 `sandbox_audit_middleware.py`，2026-06-26 漂移清单漏列、本轮发现补）：`_classify_single_command` 里 `shlex.split` 失败时由 `return 'block'` 改 `pass`。高危模式在 shlex **之前**已对原文检查，故 heredoc 体内的 `cat /etc/shadow` 仍被 block；只有合法 heredoc 才落中危检查后放行。fail-closed 不变量保住。
  - **AIO ErrorObservation 恢复重试**（`aio_sandbox.py`）：检测到 `ErrorObservation` 后的重试，由裸 `exec_command(id=fresh_id)` 改 `create_session` + `try`/`finally cleanup_session`——旧版每次错误恢复泄漏一个 session。
  - **defer**：**#3729**（`acquire(*, user_id)` + 按 `(user_id, thread_id)` 归桶 + sandbox_id 嵌 user_id + AIO backend `create(*, user_id)`/`discover` 签名）属 IM channel owner-scoping（Gateway，触及 `app/channels/feishu.py` + `app/gateway/routers/uploads.py`），同 task_tool #2676 / uploads #3579 / mcp #3294 不 port——mini 经 `get_effective_user_id()` 已正确按用户隔离路径；`tools.py` 的 ACP workspace 路径翻译 / custom mounts / MCP allowed paths（=#3597，已在 defer 清单）是 mini 未建的 additive 特性；AIO soft-load `agent_sandbox`（红线 #24，上游改硬 import）+ `%`-format 日志为 mini 已知选择 / cosmetic。
  - + 7 项 hermetic 测试：`test_sandbox_provider_lifecycle.py`（5——8 线程 barrier 冷启动见同一单例 / reset·shutdown·set 与 get 竞争绝不返回 None / 输家被 shutdown）+ `test_sandbox.py`（2——合法 heredoc 放行 / heredoc 含高危仍 block）；`test_aio_sandbox.py` 的 `_FakeShellApi` 加 `create_session`/`cleanup_session` + 恢复测试断言建/拆配对。2 篇文档：`docs/sandbox.md`（Phase 2 header + #3730 §provider 单例设计原理 + #3786 §SandboxAuditMiddleware）/ `docs/aio_sandbox.md`（Phase 2 header + ErrorObservation 恢复 session 设计原理 + #3729/soft-load defer）。基线 1651 → **1658 passed**, 0 lint。详见 §3.2（M10/M10b 行已 ✅）/ §3.1-B（#3730 行已跟进、#3729 行已定论不 port）。

---

### 🎯 当前任务（唯一在做）

**🎉 §三 全 Phase 重审已于 2026-06-29 收口**——§3.2 全部 13 行（M16 / M11 / M15 / M13+M14 / M20+M21+M23 / M12+M17+M22 / M18+M19+集成 / Phase 0 / Phase 1 / M10+M10b）均 ✅。2026-06-26 上游漂移触发的六维重审（代码对齐 + 面向小白文档 + hermetic 测试）全部完成，基线 1477 → **1658 passed**, 0 lint（+181 测试）。

**剩余工作 = 附加专项（按需，低优先）**——下面 pick-list 里各项**互相独立**、按需求触发，不再是「单任务推进制」的线性序列。挑哪一项取决于实际需求（用户要用某个 provider / 想要某个特性才做）。每项仍遵循三交付标准（代码 + hermetic 测试 + 文档）。

### 📋 附加专项 pick-list（按需，互相独立）

| 项 | 触发条件 | 规模 | 说明 |
|----|---------|------|------|
| `utils/messages.py::message_to_text` 合并 | 想统一 3 处内联等价 | 中 | 跨 M7（RunJournal）/ M13（memory）/ M16（tool_output_budget）的重构，join 语义略异；非 bug，纯整合 |
| `utils/readability.py::Article.to_message` + subprocess 提取器 | M21 多模态需要 | 中 | mini 走 `readabilipy` + `_fallback_extract`；上游用 subprocess Readability.js |
| community `fastcrw` / `groundroute` provider | 用户要用这两个搜索 provider | 各小 | 纯 httpx，最易补；mini 已有 9 个搜索 provider |
| community `#3575` Serper Images + SSRF 守卫 | 用户要 Serper 图搜 | 小 | 含专属 `_safe_public_url` SSRF 守卫 |
| mcp `#3597` stdio 产物虚拟路径翻译 | 用 stdio MCP 且产物含本地路径 | 大 | 9 函数 ~370 行：workspace 快照 + 路径重写 + 文件名去重 |
| `#3592` guaranteed memory injection | token 压力下要保底注入 correction fact | 中 | 跨 prompt.py + memory_config + lead_agent/prompt 3 模块新增特性 |
| `config/runtime_paths.py` | 要 `DEER_FLOW_HOME` env 覆盖 | 小 | mini 用 `config/paths.py`；缺 env 覆盖能力 |
| `sandbox/tools.py` ACP workspace 路径 + custom mounts | 用 ACP agent / 要自定义挂载 | 中 | M10 重审发现的上游 additive 特性（本轮 defer） |

> 【本轮】M10 / M10b sandbox 全维重审已于 2026-06-29 完成（见上「✅ 已完成」末条）；#3730 + search.py + #3786(heredoc) + AIO recovery-session 全部闭环；**§三 全 Phase 重审收口**。#3729（Gateway）定论不 port。
> 【上轮】Phase 1（M-models/M4-M9 持久化）全维重审已于 2026-06-29 完成；#3686 + serialization(Interrupt) + #3658 SQL 层 + engine + base + journal 对齐闭环。
> 【更早】Phase 0（2026-06-28）/ M18+M19+集成（2026-06-28）/ M12+M17+M22（2026-06-28）/ M20+M21+M23（2026-06-28）/ M14（2026-06-28）/ M13 / M15 / M11 / M16（2026-06-27）依次完成。
